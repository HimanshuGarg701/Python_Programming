import calendar   # importing calendar
import time       # importing time
print(calendar.month(2018,7))   # shows the current date
print(time.asctime(time.localtime(time.time())))    # shows the current itme
